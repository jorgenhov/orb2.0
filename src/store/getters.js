export const recipes = state => {
  return state.recipes;
};

export const recipesLog = state => {
  return state.recipesLog;
};

export const filteredRecipes = state => {
  return state.filteredRecipes;
};

export const optionsCategory = state => {
  return state.optionsCategory;
};

export const optionsUser = state => {
  return state.optionsUser;
};

export const selectedCategoryStore = state => {
  return state.selectedCategory;
};

export const selectedAuthorStore = state => {
  return state.selectedAuthor;
};

export const recipesLength = state => {
  return state.recipesLength;
};

export const searchTermStore = state => {
  return state.searchTerm;
};

export const isStagedRecipesSearched = state => {
  return state.isStagedRecipesSearched;
};

export const title = state => {
  return state.title;
};

export const thumbnailImage = state => {
  return state.thumbnailImage;
};

export const selectedCategoryNew = state => {
  return state.selectedCategoryNew;
};

export const selectedUserNew = state => {
  return state.selectedUserNew;
};

export const instructionsInputs = state => {
  return state.instructionsInputs;
};

export const ingredientInputs = state => {
  return state.ingredientInputs;
};
